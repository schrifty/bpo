# Map Pendo / health-report / Salesforce rollup labels to exact strings in the CS Report
# `customer` column (rows also need `delta: week`). Keys are case-insensitive.
# Salesforce often uses short rollup labels (e.g. Johnson) while CS Report uses legal names.
JCI:
  - Johnson Controls
  - Johnson Controls International
Johnson:
  - Johnson Controls
  - Johnson Controls International
  - JCI
