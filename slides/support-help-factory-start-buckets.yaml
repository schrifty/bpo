id: support_help_factory_start_buckets
type: standard
order: 24.25
title: Post-Implementation Ticket Volumes
slide_type: support_help_factory_start_buckets
data_tools:
  - customer_jira
customers: all

prompt: >
  Single column chart: HELP tickets created in each calendar-day window after Salesforce factory
  start (site-scoped org + text where configured). A red vertical slide overlay marks the hypercare
  cutoff at day 42 (“Hypercare Cutoff”). Subtitle summarizes entity coverage and Salesforce field name.
