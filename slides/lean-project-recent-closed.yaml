id: lean_project_recent_closed
type: standard
order: 98
title: Recently closed LEAN tickets
slide_type: lean_project_recent_closed
data_tools:
  - customer_jira
customers: all

prompt: >
  Lists LEAN project tickets for the scoped customer whose resolved date falls
  in the recent window (same match as ticket metrics). Newest resolved first. Ticket
  keys link to Jira when base URL is present.
