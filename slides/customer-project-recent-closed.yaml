id: customer_project_recent_closed
type: standard
order: 94
title: Recently closed CUSTOMER tickets
slide_type: customer_project_recent_closed
data_tools:
  - customer_jira
customers: all

prompt: >
  Lists CUSTOMER project tickets for the scoped customer whose resolved date falls
  in the recent window (same match as ticket metrics). Newest resolved first. Ticket
  keys link to Jira when base URL is present.
