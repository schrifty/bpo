# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environment
.venv/

# Secrets
.env
*.json
!.vscode/*.json
!config/data_field_synonyms.json
!config/data_summary_target_aliases.json
# Private key for Salesforce JWT — never commit
server.key
*.key

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Generated deck artifacts
decks/thumbnails/
slides/qbr-template-authoring-cues.yaml

# Hydrate / field-matching audit (see src/matching_log.py)
matching.log

# Slide analysis cache (classification / adapt) — avoid re-calling LLM for same slide
.slide_cache/

# Byte-compiled
*.so
src/pendo_client.py

# Local QBR explicit mapping file (delete to remap a new template; copy from config/qbr_mappings.example.yaml).
config/qbr_mappings.yaml
