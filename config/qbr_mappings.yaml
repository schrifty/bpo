# Explicit QBR hydrate mappings. After each template QBR adapt, unmapped replacement ``original``
# strings are appended under ``slides`` with ``target: ""`` unless already present (set ``target`` manually).
# Disable auto-append: BPO_QBR_MAPPINGS_AUTOWRITE=false
#
# version 2: slides[].slide_number (1-based deck index) → elements[].name / source / target
#             global_elements: any slide (optional per-row slide_id)

version: 2
slides: []
global_elements: []
