name: llm-context-portfolio-daily
# Slack + §7 LLM calls stay off on ECS/Production unless CORTEX_ALLOW_PRODUCTION_LLM_EXPORT=true.
stop_on_failure: true
fail_on_warnings: false
steps:
  - name: llm-context-export
    command: export-all
    days: 90
    require_pendo_snapshot: true
    require_pendo_windows: [90]
